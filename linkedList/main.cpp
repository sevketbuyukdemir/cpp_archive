/**
 * Created by sevket.buyukdemir
 *
 * A linked list is a linear data structure where each element is a separate object.
 *
 * node : each element in linked list
 * first/head : the address of the first node in the list is the stored in a separate location.
 *
 * Three types of linked lists :
 * 1- Ordered Linked List -> (orderedLinkedList.cpp)
 * 2- Unordered Linked List -> (unorderedLinkedList.cpp)
 * 3- Doubly Linked List (ordered or unordered) -> (doublyLinkedList.cpp)
 *
 * You can define much more linked list example you can use
 * abstract data types (generic programming) -> (unorderedLinkedList.cpp)
 *
 *
 * Necessary operations for linked lists :
 * 1- Define node struct.
 * 2- Define first, last, temp_node nodes, initialize list, define empty list..
 * 3- Define retrieve the information contained in the first and the last node
 * 4- Define traversing function
 * 5- Define item insertion function
 * 6- Define item deletion function
 * 7- Define print function
 * 8- Define ORDER function
 * 9- Define search the list for a given item
 * 10- Define make a copy of the linked list
 *
 * you can define order function or you can make ordered insertion or deletion...
 *
 * Tips :
 * You can overload operators for linked list operations examples :
 *       * operator for get temp_node info
 *       ++ operator for get next node
 *       == operator for compare node and next node
 *       != operator again compare nodes
 *
 *
 */

#include <iostream>

int main() {
    std::cout << "Linked Lists!!!" << std::endl;

    return 0;
}
